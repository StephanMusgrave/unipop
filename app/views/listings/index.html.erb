<%= link_to '+', new_listing_path %>	
<% if @all_listings.any? %>
	<% @all_listings.each do |listing| %>
		<%= listing.description %>
		<%= listing.id %>
	<% end %>
<% else %>
	There are no items yet
<% end %>